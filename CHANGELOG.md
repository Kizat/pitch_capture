## 0.0.3

* Added `Capture.audioStream(...)` for stream-based microphone consumption.
* Added protection against late native callbacks after `stop()`.
* Updated the example app to use the safer stream + `asyncMap(...)` flow.
* Expanded `README.md` with setup, usage, pitch detection, and integration notes.
